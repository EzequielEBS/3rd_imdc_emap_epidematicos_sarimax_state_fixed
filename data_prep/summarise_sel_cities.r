# summarise_sel_cities.r — Placeholder (currently empty / not implemented).
#
# Reserved for an optional diagnostic summary table over the focal cities
# selected by sel_cities.r (e.g. case counts, missingness, or descriptive
# statistics per city). It is not part of the data path required to produce
# the state-level files used by sarimax/src/model_sel.r or sarimax/src/fit.r;
# none of the other scripts in this repository depend on its output. Safe to
# ignore or to implement later.
